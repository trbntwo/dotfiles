#!/usr/bin/bash

# ls
alias ll="ls -l --color=auto"
alias lla="ls -la --color=auto"
alias la="ls -a --color=auto"

# mkdir
alias md="mkdir"

# distrobox
alias db="distrobox"
alias dbe="distrobox ephemeral"
